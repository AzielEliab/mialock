name: mialock
description: M.I.A.Lock phone shell — event map, search plans, Doe leads. Not an ID.
publish_to: "none"
version: 0.1.1+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sigil.png
